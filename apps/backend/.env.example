# ============================================================================
# SYNAPSE Backend Environment Configuration
# ============================================================================
# Copy this file to .env and replace placeholders with actual values
# DO NOT commit .env to version control
# WARNING: Keep SERVICE_ROLE_KEY secret - it bypasses RLS policies!

# PostgreSQL Database Connection
# Get from: Supabase Dashboard → Project Settings → Database → Connection String
# Format: postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres
DATABASE_URL=postgresql://postgres:lrRh7ey99IDkwglx@db.qdvxjacpylqsstsjttam.supabase.co:5432/postgres


# Supabase Project URL
# Same as frontend NEXT_PUBLIC_SUPABASE_URL
# Format: https://[project-ref].supabase.co
SUPABASE_URL=https://qdvxjacpylqsstsjttam.supabase.co

# Supabase Service Role Key (Secret)
# WARNING: This key bypasses Row Level Security - NEVER expose to frontend!
# Get from: Supabase Dashboard → Project Settings → API → service_role key
# Use only for server-side admin operations
SUPABASE_SERVICE_ROLE_KEY=sb_secret_N-UTFbbCP_BNgjQ1qOrX1Q_udOLsoO1

# ============================================================================
# Optional: Application Configuration
# ============================================================================

# API Server Port (default: 8000)
# PORT=8000

# Environment (development, staging, production)
# ENVIRONMENT=development

# CORS Allowed Origins (comma-separated)
# CORS_ORIGINS=http://localhost:3000,https://your-frontend-domain.com

# ============================================================================
# Setup Instructions:
# ============================================================================
# 1. Create a Supabase project at https://supabase.com
# 2. Navigate to Project Settings → Database
# 3. Copy the Connection String and replace [password] with your database password
# 4. Navigate to Project Settings → API
# 5. Copy the Project URL to SUPABASE_URL
# 6. Copy the service_role key to SUPABASE_SERVICE_ROLE_KEY
# 7. Copy this file to .env
# 8. Replace all placeholder values with your actual credentials
# 9. Verify .env is in .gitignore
# 10. NEVER commit .env or share SERVICE_ROLE_KEY publicly
# ============================================================================

# ============================================================================
# Security Notes:
# ============================================================================
# - DATABASE_URL: Contains database password - keep secret
# - SUPABASE_SERVICE_ROLE_KEY: Bypasses RLS - use only for admin operations
# - For client-side operations, use the anon key (in frontend .env.local)
# - Always use parameterized queries to prevent SQL injection
# - Store credentials in secure vault for production deployments
# ============================================================================
